<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Thryft" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Thryft">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="main-navigation">
      <div class="nav-container">
        <div class="nav-brand">
          <%= link_to 'Thryft', root_path, class: 'brand-link' %>
        </div>
        <div class="nav-links">
          <% if current_user %>
            <%= link_to 'Listings', listings_path, class: 'nav-link' %>
            <%= link_to 'My Purchases', purchases_path, class: 'nav-link' %>
            <% if current_user.moderator? %>
              <%= link_to 'User Management', users_path, class: 'nav-link moderator-link' %>
              <%= link_to 'Reported Listings', reported_listings_path, class: 'nav-link moderator-link' %>
            <% end %>
            <%= button_to 'Log out', logout_path, method: :delete, class: 'nav-link logout-btn', form: { class: 'logout-form' } %>
          <% else %>
            <%= link_to 'Log in', login_path, class: 'nav-link' %>
          <% end %>
        </div>
      </div>
    </nav>

    <% if notice.present? %>
      <p class="notice"><%= notice %></p >
    <% end %>
    <% if alert.present? %>
      <p class="alert"><%= alert %></p >
    <% end %>
    <% if flash[:error].present? %>
      <p class="error"><%= flash[:error] %></p >
    <% end %>

    <div class="main-content">
      <%= yield %>
    </div>
  </body>
</html>